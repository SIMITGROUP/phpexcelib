// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("excel", "for excel support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("excel", "enable excel support", "no");

if (PHP_EXCEL != "no") {
	EXTENSION("excel", "excel.cpp");
}

